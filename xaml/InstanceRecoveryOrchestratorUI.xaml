<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:term="clr-namespace:EasyWindowsTerminalControl;assembly=EasyWindowsTerminalControl"
        Title="VCF Instance Recovery Orchestrator" Height="800" Width="1100">
    <!--
        Loaded via XamlReader.Load from Start-InstanceRecoveryUI (see the .psm1).
        There is no code-behind class, so this file deliberately has no x:Class, no inline event
        handler attributes (Click=/SelectionChanged=), and no data-binding markup on the list
        boxes. All wiring and list population happens imperatively in the PowerShell loader script.
    -->
    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#2D6CDF" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#1F54B8" />
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#F3F5F9" />
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#DEE2EA" />
        <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#FAFBFD" />
        <SolidColorBrush x:Key="HeaderTextBrush" Color="#1F2430" />
        <SolidColorBrush x:Key="SubtleTextBrush" Color="#6B7280" />
        <SolidColorBrush x:Key="HoverBackgroundBrush" Color="#EEF3FC" />

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource HeaderTextBrush}" />
        </Style>

        <!-- Default GroupBox chrome (a sunken gray box) is the single most dated-looking WPF
             control; this replaces it with a flat card: white panel, subtle border, a distinct
             header bar instead of an inline caption cutting into the border line. -->
        <Style TargetType="GroupBox">
            <Setter Property="Padding" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{StaticResource PanelBackgroundBrush}"
                                BorderBrush="{StaticResource PanelBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Border Grid.Row="0"
                                        Background="{StaticResource HeaderBackgroundBrush}"
                                        BorderBrush="{StaticResource PanelBorderBrush}"
                                        BorderThickness="0,0,0,1"
                                        CornerRadius="6,6,0,0">
                                    <TextBlock Text="{TemplateBinding Header}"
                                               Foreground="{StaticResource HeaderTextBrush}"
                                               FontWeight="SemiBold"
                                               FontSize="12"
                                               Margin="12,7,12,7" />
                                </Border>
                                <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <!-- Flat hover/selected highlight instead of the default gray selection box + dotted
             focus rectangle. -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="4,3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="4">
                            <ContentPresenter Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Flat underline tabs instead of the default 3D-look tab chrome. -->
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" Background="Transparent" BorderBrush="{StaticResource AccentBrush}" BorderThickness="0,0,0,2" Margin="0,0,4,0">
                            <ContentPresenter ContentSource="Header" Margin="10,6,10,6" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="TabBorder" Property="BorderThickness" Value="0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Data source (left) + workload domains (right) -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Data Source">
                <StackPanel>
                    <RadioButton x:Name="ExtractBackupRadio" Content="Extract SDDC Manager Backup" GroupName="DataSource" Margin="0,0,0,6" />
                    <RadioButton x:Name="UseExtractedDataRadio" Content="Load Extracted SDDC Data" GroupName="DataSource" IsChecked="True" Margin="0,0,0,6" />
                    <DockPanel Margin="0,4,0,0">
                        <Button x:Name="BrowseButton" Content="Browse..." Width="80" DockPanel.Dock="Right" />
                        <TextBox x:Name="FilePathTextBox" IsReadOnly="True" Margin="0,0,8,0" VerticalContentAlignment="Center" />
                    </DockPanel>
                    <TextBlock x:Name="StatusTextBlock" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="#C0392B" />
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Column="2" Header="Workload Domains">
                <ListBox x:Name="WorkloadDomainsListBox" />
            </GroupBox>
        </Grid>

        <!--
            Steps (left) + Variables (right) for the selected domain. Collapsed until a domain is
            picked (see the .psm1's domain SelectionChanged handler). Steps show only the cmdlet
            name; the full command line with its parameters lives in the .psm1 and is never
            rendered here. Variables shows only the currently active tab's own variables, rebuilt on
            tab switch (see Update-VariablesForActiveTab). Both lists are capped to roughly 10 rows
            with a scrollbar for the rest.
        -->
        <Grid Grid.Row="1" x:Name="PhasesRow" Margin="0,0,0,12" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Steps">
                <TabControl x:Name="StepsTabControl" Background="Transparent" BorderThickness="0">
                    <TabItem Header="Management Domain Restores">
                        <ListBox x:Name="ManagementDomainRestoresStepsListBox" MaxHeight="240" ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="0,8,0,0" />
                    </TabItem>
                    <TabItem Header="Recover Default Cluster">
                        <ListBox x:Name="RecoverDefaultClusterStepsListBox" MaxHeight="240" ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="0,8,0,0" />
                    </TabItem>
                </TabControl>
            </GroupBox>

            <GroupBox Grid.Column="2" Header="Variables">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="240">
                    <StackPanel x:Name="VariablesPanel" />
                </ScrollViewer>
            </GroupBox>
        </Grid>

        <!-- Embedded console -->
        <GroupBox Grid.Row="2" Header="Console">
            <term:EasyTerminalControl x:Name="Term" StartupCommandLine="pwsh.exe -NoLogo -NoExit" />
        </GroupBox>
    </Grid>
</Window>
